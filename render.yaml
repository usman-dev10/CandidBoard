services:
  - type: web
    name: candidboard-api
    runtime: python
    buildCommand: pip install -r Backend/requirements.txt
    startCommand: cd Backend && uvicorn src.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.11
